<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE struts PUBLIC
	"-//Apache Software Foundation//DTD Struts Configuration 2.3//EN"
	"http://struts.apache.org/dtds/struts-2.3.dtd">

<struts>

    <package name="interceptorpkg" extends="struts-default" namespace="/interceptor">
        <interceptors>
            <!--注册拦截器-->
            <interceptor name="checkLogin" class="com.coderZsq.interceptor.CheckLoginInterceptor">
                <!--设置不被检查的action名称, 使用,隔开-->
                <param name="actionNames">login,logon</param>
            </interceptor>
            <!--拦截器栈: 多个拦截器的集合-->
            <interceptor-stack name="myStack">
                <interceptor-ref name="checkLogin"/>
                <interceptor-ref name="defaultStack"/>
            </interceptor-stack>
        </interceptors>

        <!--设置当前package的默认引用拦截器-->
        <default-interceptor-ref name="myStack"/>

        <!--全局的结果视图-->
        <global-results>
            <result name="login" type="redirect">/views/interceptor/login.jsp</result>
        </global-results>

        <action name="login" class="com.coderZsq.interceptor.LoginAction">
            <!--引用自己的拦截器-->
            <interceptor-ref name="defaultStack"/>
            <result type="redirectAction">
                <param name="actionName">main</param>
            </result>
        </action>

        <action name="main">
            <!--引用拦截器-->
            <!--<interceptor-ref name="checkLogin"/>-->
            <result>/views/interceptor/welcome.jsp</result>
        </action>
        <action name="other">
            <result>/views/interceptor/welcome.jsp</result>
        </action>
        <action name="abc">
            <result>/views/interceptor/welcome.jsp</result>
        </action>
    </package>

</struts>
